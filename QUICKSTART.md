# 🚀 Быстрый старт Price FamQ Bot

## За 5 минут до запуска

### 1️⃣ Установка (1 минута)
```bash
pip install -r requirements.txt
```

### 2️⃣ Создание бота (2 минуты)
1. Идите на https://discord.com/developers/applications
2. New Application → Add Bot
3. **ВАЖНО:** Включите все 3 Intents!
4. Скопируйте токен

### 3️⃣ Настройка токена (30 секунд)
```bash
# Создайте .env файл из примера
cp .env.example .env

# Откройте .env и вставьте токен:
DISCORD_BOT_TOKEN=ваш_токен_здесь
```

### 4️⃣ Запуск (10 секунд)
```bash
python main.py
```

---

## Настройка на сервере (5 минут)

### Шаг 1: Создайте каналы
- `#приветствие`
- `#заявки`
- `#рассмотрение-заявок` (приватный)
- `#логи` (приватный)

### Шаг 2: Создайте роли
- `Модератор` (для рассмотрения заявок)
- `Участник` (выдается при принятии)
- `Dev` (доступ к логам)
- `Owner` (полный доступ)

### Шаг 3: Настройте бота
```
!config welcome_channel #приветствие
!config application_channel #заявки
!config review_channel #рассмотрение-заявок
!config logs_channel #логи
!config moderator_role @Модератор
!config member_role @Участник
!config dev_role @Dev
!config owner_role @Owner
!config logo https://ваш-логотип.png
```

### Шаг 4: Создайте кнопку заявки
В канале `#заявки`:
```
!setup_application
```

---

## ✅ Готово!

Теперь бот:
- ✨ Приветствует новых участников
- 📝 Принимает заявки через форму
- 👀 Позволяет модераторам рассматривать заявки
- 📊 Логирует все действия
- 💾 Позволяет Dev/Owner скачивать логи

---

## 📚 Основные команды

| Команда | Для кого | Описание |
|---------|----------|----------|
| `!config` | Admin | Настройки бота |
| `!setup_application` | Admin | Создать кнопку заявки |
| `!help` | Все | Список команд |
| `!download_logs [дни]` | Dev/Owner | Скачать логи |
| `!logs_stats` | Dev/Owner | Статистика логов |
| `!clear_logs` | Owner | Очистить логи |

---

## 🔑 Важные моменты

### ⚠️ Обязательно включите Intents!
В Discord Developer Portal → Bot → Privileged Gateway Intents:
- ✅ PRESENCE INTENT
- ✅ SERVER MEMBERS INTENT  
- ✅ MESSAGE CONTENT INTENT

### 🔒 Права бота
Роль бота должна быть выше роли "Участник" в списке ролей!

### 💾 Логи
Только роли Dev и Owner могут скачивать логи:
```
!download_logs      # Последние 7 дней
!download_logs 30   # Последние 30 дней
```

---

## 🎨 Структура модулей

```
main.py                  # Главный файл
├── cogs/welcome.py      # Приветствия
├── cogs/applications.py # Заявки
├── cogs/logs.py         # Логирование + скачивание
└── cogs/config_commands.py # Команды настройки
```

---

## ❓ Проблемы?

**Бот не отвечает?**
→ Проверьте Intents в Developer Portal

**Кнопки не работают?**
→ Используйте `!setup_application` заново

**Не могу скачать логи?**
→ Убедитесь что у вас роль Dev или Owner

**Подробная документация:** README.md

---

**Удачи! 🎮**