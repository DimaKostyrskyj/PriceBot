# 📋 Система контрактов - Обновленная инструкция

## 🆕 Что нового:

### ✨ Фиксированная кнопка
- Кнопка "Запросить контракт" **автоматически закрепляется**
- Перезакрепление каждые **3 часа**
- Старые сообщения **не удаляются**

### 🎯 Modal для создания контракта
- Slash-команда `/contract`
- Красивая форма Discord
- Автоматическая публикация

---

## ⚙️ Настройка (обновленная):

### Шаг 1: Создайте каналы
```
#контракты-запросы    - для запросов от пользователей
#контракты-members    - для публикации контрактов
```

### Шаг 2: Настройте бота
```bash
!config contracts_channel #контракты-запросы
!config contracts_members_channel #контракты-members
```

### Шаг 3: Создайте кнопку запроса
В канале `#контракты-запросы`:
```bash
!setup_contract_request
```
**Кнопка будет автоматически закреплена и перезакрепляться каждые 3 часа!**

### Шаг 4: Отправьте информацию в Members (опционально)
```bash
!setup_contract_info
```

---

## 📝 Команды:

### Новая slash-команда ⭐

**`/contract`**
- Открывает форму Discord Modal
- Заполните 5 полей
- Контракт автоматически публикуется в Members
- **Используйте это вместо старой команды!**

### Старые команды (все еще работают)

**`!setup_contract_request`**
- Создает фиксированную кнопку
- Автоматически закрепляет
- Перезакрепление каждые 3 часа

**`!publish_contract "название" "срок" "время" "за сколько" "процент"`**
- Старый способ публикации
- Работает, но лучше использовать `/contract`

**`!setup_contract_info`**
- Отправляет информационное сообщение

---

## 🎮 Как это работает:

### Для пользователей (запрос):

1. Заходят в `#контракты-запросы`
2. Видят **закрепленную кнопку** "📝 Запросить контракт"
3. Нажимают на кнопку
4. Заполняют форму (5 полей)
5. Запрос публикуется в канале

**Преимущество:** Кнопка всегда закреплена, легко найти!

### Для администраторов (публикация):

**Новый способ (рекомендуется):**
1. Используйте `/contract` (slash-команда)
2. Заполните форму Modal
3. Нажмите "Отправить"
4. Готово!

**Старый способ:**
```bash
!publish_contract "50 вексельок" "до 25.12.2024" "от 14:30 до 24:30" "от 4ч до 12ч" "100%"
```

---

## 🔄 Автозакрепление кнопки:

### Как работает:

- Кнопка создается командой `!setup_contract_request`
- **Автоматически закрепляется** при создании
- Каждые **3 часа** бот проверяет закрепление
- Если кнопка откреплена - закрепляет заново
- **Старые сообщения НЕ удаляются!**

### Преимущества:

✅ Кнопка всегда на виду
✅ Пользователи легко находят
✅ Не нужно вручную закреплять
✅ История запросов сохраняется

---

## 💡 Примеры использования:

### Пример 1: Создание контракта через Modal
1. Напишите `/contract`
2. Откроется форма
3. Заполните:
   - Название: `50 вексельок`
   - Срок: `до 25.12.2024`
   - Время выполнения: `от 14:30 до 24:30`
   - За какое время: `от 4ч до 12ч`
   - Процент: `100%`
4. Отправьте
5. Готово!

### Пример 2: Старый способ
```bash
!publish_contract "30 вексельок" "до 20.12.2024" "от 12:00 до 22:00" "от 3ч до 8ч" "100%"
```

---

## 🎯 Рабочий процесс:

```
Пользователь видит закрепленную кнопку
         ↓
Нажимает "Запросить контракт"
         ↓
Заполняет форму
         ↓
Запрос появляется в #контракты-запросы
         ↓
Администратор просматривает
         ↓
Администратор использует /contract
         ↓
Заполняет Modal форму
         ↓
Контракт публикуется в #контракты-members
         ↓
Members видят и записываются
```

---

## ⚙️ Технические детали:

### Автозакрепление:
- **Интервал:** 3 часа
- **Метод:** Discord Tasks
- **Поведение:** Проверка и перезакрепление
- **Старые сообщения:** Остаются в канале

### Modal форма:
- **Тип:** Discord Modal
- **Поля:** 5 TextInput
- **Лимиты:** 50-100 символов
- **Отправка:** Автоматическая публикация

---

## 📊 Сравнение способов:

| Функция | Старый способ | Новый способ |
|---------|--------------|--------------|
| Команда | `!publish_contract` | `/contract` |
| Интерфейс | Текст в кавычках | Discord Modal |
| Удобство | Средне | Отлично |
| Ошибки | Легко ошибиться | Валидация |
| Кнопка | Не закрепляется | Автозакрепление |

**Рекомендуем использовать новый способ!**

---

## ❓ FAQ:

**Q: Как часто закрепляется кнопка?**
A: Каждые 3 часа. При создании закрепляется сразу.

**Q: Удаляются ли старые запросы?**
A: Нет! Все сообщения остаются в канале.

**Q: Можно ли изменить интервал закрепления?**
A: Да, в коде измените `@tasks.loop(hours=3)` на нужное значение.

**Q: Что лучше: /contract или !publish_contract?**
A: `/contract` - удобнее и проще!

**Q: Работает ли старая команда?**
A: Да, `!publish_contract` все еще работает.

---

## 🚀 Быстрая настройка:

```bash
# 1. Создайте каналы
#контракты-запросы
#контракты-members

# 2. Настройте бота
!config contracts_channel #контракты-запросы
!config contracts_members_channel #контракты-members

# 3. Создайте фиксированную кнопку
# В канале #контракты-запросы:
!setup_contract_request

# 4. Готово! Теперь можно:
# - Пользователи запрашивают через кнопку
# - Вы публикуете через /contract
```

---

## 🎯 Проверка работы:

### Тест 1: Фиксированная кнопка
1. Зайдите в #контракты-запросы
2. Убедитесь что кнопка закреплена
3. Нажмите кнопку
4. Заполните форму
5. Проверьте что запрос появился

### Тест 2: Slash-команда /contract
1. Напишите `/contract`
2. Заполните Modal форму
3. Отправьте
4. Проверьте #контракты-members
5. Убедитесь что контракт опубликован

### Тест 3: Автозакрепление
1. Открепите кнопку вручную
2. Подождите 3 часа (или перезапустите бота)
3. Проверьте что кнопка закреплена снова

---

## 🔧 Устранение проблем:

**Проблема:** Кнопка не закрепляется
**Решение:** Проверьте права бота (Manage Messages)

**Проблема:** /contract не работает
**Решение:** Перезапустите бота, команды синхронизируются автоматически

**Проблема:** Форма не отправляется
**Решение:** Проверьте настройку каналов через `!config`

---

**Система обновлена! Наслаждайтесь новыми возможностями! 🎮**
