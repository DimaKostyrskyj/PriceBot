# 🤖 Price FamQ Discord Bot

Модульный Discord бот для управления семьей Price FamQ на GTA 5 RP сервере с системой приветствий, заявок и логов.

## 📋 Возможности

### ✨ Модуль приветствия (`cogs/welcome.py`)
- Автоматическое приветствие новых участников
- Красивые embed с логотипом семьи
- Информация о семье и процессе вступления
- Логирование выхода участников

### 📝 Модуль заявок (`cogs/applications.py`)
- Форма заявки с 5 полями
- Кнопка для подачи заявки
- Система рассмотрения с 3 кнопками:
  - 📋 Рассмотреть
  - ✅ Одобрить (автоматическая выдача роли)
  - ❌ Отклонить (с указанием причины)
- Уведомления в ЛС пользователям
- Логирование всех действий

### 📊 Модуль логов (`cogs/logs.py`)
- Логирование удаленных сообщений
- Логирование отредактированных сообщений
- Логирование банов/разбанов
- Логирование создания/удаления каналов и ролей
- **Скачивание логов** (только для Dev и Owner ролей)
- Статистика логов
- Очистка логов с резервной копией

### ⚙️ Модуль настройки (`cogs/config_commands.py`)
- Команды настройки всех параметров
- Управление ролями и каналами
- Перезагрузка конфигурации
- Система помощи

## 🚀 Установка

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 2: Создание бота в Discord

1. Перейдите на [Discord Developer Portal](https://discord.com/developers/applications)
2. Нажмите "New Application"
3. Придумайте название (например: "Price FamQ Bot")
4. Перейдите в раздел "Bot"
5. Нажмите "Add Bot"
6. **ВАЖНО!** Включите все 3 Privileged Gateway Intents:
   - ✅ PRESENCE INTENT
   - ✅ SERVER MEMBERS INTENT
   - ✅ MESSAGE CONTENT INTENT
7. Нажмите "Reset Token" и скопируйте токен

### Шаг 3: Настройка токена

1. Скопируйте файл `.env.example` и переименуйте в `.env`:
```bash
cp .env.example .env
```

2. Откройте `.env` и вставьте ваш токен:
```env
DISCORD_BOT_TOKEN=ваш_токен_бота_здесь
```

### Шаг 4: Добавление бота на сервер

1. В Developer Portal перейдите в "OAuth2" → "URL Generator"
2. Выберите scope: `bot` и `applications.commands`
3. Выберите права (Bot Permissions):
   - ✅ Read Messages/View Channels
   - ✅ Send Messages
   - ✅ Embed Links
   - ✅ Attach Files
   - ✅ Read Message History
   - ✅ Add Reactions
   - ✅ Manage Roles
   - ✅ Manage Messages
4. Скопируйте ссылку и откройте в браузере
5. Выберите ваш сервер

### Шаг 5: Запуск бота

```bash
python main.py
```

## ⚙️ Настройка бота на сервере

### 1. Создайте необходимые каналы:
- `#приветствие` - для приветствия новых участников
- `#заявки` - для подачи заявок
- `#рассмотрение-заявок` - приватный канал для администрации
- `#логи` - приватный канал для логов

### 2. Создайте необходимые роли:
- `Модератор` или `Администратор` - для рассмотрения заявок
- `Участник` или `Price FamQ` - для членов семьи
- `Dev` - для разработчиков (доступ к логам)
- `Owner` - для владельцев (полный доступ)

### 3. Настройте каналы:
```
!config welcome_channel #приветствие
!config application_channel #заявки
!config review_channel #рассмотрение-заявок
!config logs_channel #логи
```

### 4. Настройте роли:
```
!config moderator_role @Модератор
!config member_role @Участник
!config dev_role @Dev
!config owner_role @Owner
```

### 5. Установите логотип:
```
!config logo https://ссылка-на-ваш-логотип.png
```

### 6. Создайте кнопку заявки:
В канале `#заявки` напишите:
```
!setup_application
```

## 📖 Команды

### Команды администратора
| Команда | Описание |
|---------|----------|
| `!config` | Показать текущие настройки |
| `!config <setting> <value>` | Изменить настройку |
| `!setup_application` | Создать кнопку заявки |
| `!reload` | Перезагрузить конфигурацию |
| `!status` | Показать статус бота |
| `!help` | Список всех команд |

### Команды для Dev и Owner
| Команда | Описание | Права |
|---------|----------|-------|
| `!download_logs [дни]` | Скачать логи за N дней (по умолчанию 7) | Dev, Owner |
| `!logs_stats` | Показать статистику логов | Dev, Owner |
| `!clear_logs` | Очистить все логи (создает резервную копию) | Owner |

### Примеры использования логов:

```bash
# Скачать логи за последние 7 дней (по умолчанию)
!download_logs

# Скачать логи за последние 30 дней
!download_logs 30

# Посмотреть статистику
!logs_stats

# Очистить все логи (только Owner)
!clear_logs
```

## 📁 Структура проекта

```
price_famq_bot/
│
├── main.py                      # Главный файл запуска
├── config.json                  # Конфигурация (создается автоматически)
├── .env                         # Токен бота (создайте из .env.example)
├── .env.example                 # Пример файла с токеном
├── requirements.txt             # Зависимости Python
├── .gitignore                   # Игнорируемые файлы
│
├── cogs/                        # Модули бота
│   ├── welcome.py              # Модуль приветствия
│   ├── applications.py         # Модуль заявок
│   ├── logs.py                 # Модуль логирования
│   └── config_commands.py      # Модуль команд настройки
│
├── utils/                       # Утилиты
│   └── config_manager.py       # Менеджер конфигурации
│
└── logs/                        # Папка с логами (создается автоматически)
    └── bot_logs.json           # Файл логов
```

## 🔒 Система прав

### Роли и их права:

| Роль | Права |
|------|-------|
| **Administrator** | Все команды настройки (!config, !setup_application, !reload) |
| **Модератор** | Рассмотрение заявок (кнопки в канале рассмотрения) |
| **Dev** | Скачивание логов, просмотр статистики |
| **Owner** | Все права Dev + очистка логов |
| **Участник** | Автоматически выдается при одобрении заявки |

## 📊 Что логируется

Модуль логов записывает следующие события:

- 🗑️ Удаленные сообщения
- ✏️ Отредактированные сообщения
- 🔨 Баны пользователей
- ✅ Разбаны пользователей
- ➕ Создание каналов
- ➖ Удаление каналов
- 🎭 Создание ролей
- 🎭 Удаление ролей
- 📝 Подача заявок
- 👀 Взятие заявок на рассмотрение
- ✅ Одобрение заявок
- ❌ Отклонение заявок
- 📥 Скачивание логов
- 🗑️ Очистка логов

Все логи сохраняются в `logs/bot_logs.json` в формате JSON.

## 🎨 Кастомизация

### Изменение цветов

Откройте `config.json` и измените цвета:

```json
{
    "colors": {
        "primary": "0xFFD700",    // Золотой - основной цвет
        "success": "0x00FF00",    // Зеленый - успех
        "error": "0xFF0000",      // Красный - ошибка
        "warning": "0xFFA500",    // Оранжевый - предупреждение
        "info": "0x00BFFF"        // Голубой - информация
    }
}
```

### Изменение префикса команд

В файле `.env`:
```env
BOT_PREFIX=!
```

Или в `config.json`:
```json
{
    "prefix": "!"
}
```

## ❓ Частые вопросы

**Q: Бот не отвечает на команды**
A: Убедитесь что:
- Все 3 Intents включены в Developer Portal
- У бота есть все необходимые права
- Токен правильно указан в .env файле

**Q: Кнопки не работают после перезапуска**
A: Используйте команду `!setup_application` чтобы создать новое сообщение с кнопкой

**Q: Не могу скачать логи**
A: Убедитесь что у вас есть роль Dev или Owner, настроенная командой `!config dev_role` или `!config owner_role`

**Q: Логи не записываются**
A: Проверьте что:
- Канал логов настроен: `!config logs_channel #логи`
- У бота есть права на отправку сообщений в канал
- Папка `logs/` создана (создается автоматически)

**Q: Как добавить больше модераторов?**
A: Используйте команду несколько раз:
```
!config moderator_role @Модератор1
!config moderator_role @Модератор2
```

**Q: Как удалить роль из списка?**
A: Используйте команды удаления:
```
!config remove_moderator @Роль
!config remove_dev @Роль
!config remove_owner @Роль
```

## 🔧 Устранение неполадок

### Бот не запускается
1. Проверьте что установлены все зависимости: `pip install -r requirements.txt`
2. Проверьте что токен указан в `.env` файле
3. Проверьте что Python версии 3.8+

### Логи не сохраняются
1. Проверьте права на запись в папку `logs/`
2. Проверьте что файл `logs/bot_logs.json` существует
3. Проверьте консоль на ошибки

### Не выдается роль при одобрении
1. Убедитесь что роль настроена: `!config member_role @Роль`
2. Проверьте что роль бота выше роли участника в иерархии
3. Убедитесь что у бота есть право "Manage Roles"

## 📝 Логи в JSON формате

Пример структуры лога:

```json
{
    "timestamp": "2024-11-25T21:30:00.000000",
    "type": "message_delete",
    "data": {
        "user_id": 123456789,
        "user_name": "Username",
        "channel_id": 987654321,
        "channel_name": "general",
        "content": "Deleted message content"
    }
}
```

## 🚀 Запуск в продакшене

### Linux (systemd)

Создайте файл `/etc/systemd/system/price-famq-bot.service`:

```ini
[Unit]
Description=Price FamQ Discord Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/price_famq_bot
ExecStart=/usr/bin/python3 /path/to/price_famq_bot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Команды:
```bash
sudo systemctl enable price-famq-bot
sudo systemctl start price-famq-bot
sudo systemctl status price-famq-bot
```

### Windows (Task Scheduler)

1. Откройте Task Scheduler
2. Создайте новую задачу
3. Настройте запуск `python main.py` при старте системы
4. Установите перезапуск при сбое

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Проверьте файл `logs/bot_logs.json`
3. Убедитесь что все настройки выполнены
4. Проверьте права бота на сервере

## 📄 Лицензия

Создано специально для семьи Price FamQ на GTA 5 RP сервере.

---

**Приятного использования! Удачи вашей семье! 🎮**